cat to only read
cat >> "filename" enter and content to append means to add to the file

for finding the word 
cat "filename" | grep -i "word" [1;

for counting the word
cat "filename" | grep -i "word" | wc -l

